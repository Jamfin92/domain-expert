/**
 * A module-scope call in a file no component reaches: matched to a route, but
 * attributed to nothing. The unattributed bucket's positive control.
 */
export const firstPage = fetch("/api/cards");
